<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hikmetanil.javatokotlin">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".P000Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".P001Intent"></activity>
        <activity android:name=".P001IntentYakala"></activity>
        <activity android:name=".P002TextPlay"></activity>
        <activity android:name=".P003MenuItem"></activity>
        <activity android:name=".P004CreateUserface"></activity>
    </application>

</manifest>